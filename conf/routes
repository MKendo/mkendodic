GET        /                       controllers.HomeController.index

#->         /v1/posts               v1.post.PostRouter

# 剑道辞典
GET        /search/:key           controllers.SearchDirctionaryControlller.search(key:String)

GET        /dailyword             controllers.DailyWordController.search()

GET        /dailyword/show        controllers.DailyWordController.show()

# 剑道词条（可翻译为中文）
GET        /kendoentry/tochinese  controllers.KendoEntryController.show()

# 会员管理
# 查询指定会员的有效期
GET        /user/validate/:userid  controllers.SearchValidateController.searchVallidate(userid:String)
# 续费，更新有效期并说明最新的续费情况
GET        /user/validateupdate  controllers.UpdateValidateController.searchVallidate()
# 新的续费
GET       /user/validate2     controllers.ValidateController.searchValidates
POST      /user/validate2     controllers.ValidateController.createValidateDescription

# 微信接口
# GET        /wx/mkserver              controllers.wx.CheckSignatureController.checksg() # 签名认证
GET        /wx/mkserver            controllers.wx.WxSearchValidateController.searchVallidate()

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file          controllers.Assets.at(path="/public", file)

